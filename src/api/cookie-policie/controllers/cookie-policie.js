'use strict';

/**
 * cookie-policie controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cookie-policie.cookie-policie');
